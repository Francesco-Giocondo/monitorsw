import string
import urllib.request

import requests
from lxml import html
import shutil

f1 = open('file1','r')
f2 = open('file2','wb')

for line in f1:
 print (line)
 with urllib.request.urlopen(line) as response:
  first = '<title>'
  last = '</title>'
  if '

# copiare nel file. importa shutil
  shutil.copyfileobj(response, f2)
